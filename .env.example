# Copy to .env.local for local dev (Next.js + npm run db:*)
HOME_TIMEZONE=
OLLAMA_HOST=
OLLAMA_MODEL=
# Optional — e.g. "South Indian or Maharashtrian"
CUISINE_CONTEXT=
# Optional — household-specific Ollama prompt line (see household-context.mdc)
OLLAMA_HOUSEHOLD_PROMPT=
DATABASE_URL=
