# Copy this file to `.env` in the project root (Prisma CLI and Prisma Studio load it automatically).
# SQLite file URL for Prisma — path is relative to the directory from which you run `npx prisma` (project root).
DATABASE_URL="file:./prisma/dev.db"
